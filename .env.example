# ── 密钥 / 环境变量（本技能默认无需任何密钥，此文件仅为示例规范）────────────
# 如果你自行扩展本技能（例如接入云端 OCR、在线文档 API），
# 请把真实密钥写入本地 .env（已被 .gitignore 忽略，切勿提交），
# 并在代码中通过 os.environ 读取。

# 示例：云端 OCR 服务密钥（本技能未使用，仅演示格式）
# EXAMPLE_OCR_API_KEY=your_api_key_here
# EXAMPLE_OCR_API_SECRET=your_api_secret_here
