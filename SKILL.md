---
name: zongce-proof-compiler
description: 大学综合素质测评（综测）活动证明材料整理。当用户提供综测评定细则、本人姓名和活动证明文件（图片/PDF/Word），需要从证明文件中定位包含该姓名的页、以整页图片提取、按细则分类并生成带加分汇总表的证明汇总 Word 时使用。触发词：综测、综合测评、综测证明、活动证明汇总、加分证明整理。
agent_created: true
---

# 综测活动证明汇总

## 用途

根据用户每次提供的《综测评定细则》、本人姓名和一批活动证明文件（图片 / PDF / Word，一份文件可能含多份不同活动的证明），完成：

1. 从每份文件中定位包含用户姓名的页，导出为整页图片（一份证明跨两页时两页都保留）；
2. 阅读每份证明内容，按《细则》分类并逐条判定活动名称与分值；
3. 生成一份汇总 Word：第 1 页为加分汇总表（含合计），之后按细则分类分章，每份证明一页一图、页首带标题。

## 输入要求

运行前必须集齐三项输入，缺一即向用户索取：

- 《综测评定细则》（任意格式：Word / PDF / 图片 / 文本）
- 用户姓名（精确到字，用于页面定位）
- 活动证明文件（图片 / PDF / Word，可多份）

## 运行环境

- Python 3.10+，安装依赖：`pip install -r requirements.txt`（推荐使用虚拟环境）；
- Word 转 PDF 依赖本机 Microsoft Word（Windows，经 docx2pdf / pywin32 调用）；无 Word 环境时 .docx 输入不可用，PDF 与图片输入不受影响；
- 本 skill 不需要任何 API Key / Token / 网络访问，全部处理在本地完成。

## 工作流程

### 1. 解析细则

通读用户提供的《综测评定细则》，提取：

- 分类体系（如"一、思想品德""二、学科竞赛"……，保留原文编号与名称）；
- 每类下的加分规则（什么级别 / 类型的活动加多少分、封顶规则、不计分项）。

若细则表述含糊（如同一活动可能落入两类、分值档位无法判定），先向用户确认，不要自行猜测。

### 2. 提取含姓名的页面（scripts/extract_pages.py）

```bash
python scripts/extract_pages.py --outdir <工作目录>/extracted --name <姓名> --neighbors <文件1> <文件2> ...
```

脚本行为：

- **PDF**：先在文字层搜索姓名，命中页 + 相邻页（`--neighbors`）渲染为 PNG；无文字层（扫描件）或文字层搜不到姓名时，渲染全部页面（上限 30 页）并标记 `needs_visual=true`；
- **Word**（.docx/.doc）：自动经本机 Word 转为 PDF 后按 PDF 流程处理；
- **图片**：直接作为单页候选；
- 输出 `<outdir>/manifest.json`，逐文件记录总页数、文字命中页、已渲染图片路径、是否需视觉识别。

需要补渲染指定页时：

```bash
python scripts/extract_pages.py --outdir <同目录> --render "<文件.pdf>:2,3"
```

### 2b. 大型扫描件 OCR 预筛（scripts/ocr_scan.py）

当 extract_pages 报告 `needs_visual=true` 且文件页数多（几十上百页）时，禁止逐页人工翻看，先做 OCR 预筛：

```bash
python scripts/ocr_scan.py --name <姓名> --dpi 200 --out ocr_hits.json <扫描版PDF...>
```

- 输出每个文件的 `hits`（全名精确/宽松命中页）、`partial_hits`（姓+名首字或名尾两字命中页）及命中行片段；
- `hits` 页 → 渲染后人工确认；`partial_hits` 页 → 必须人工核对，常见为形近异名（如"刘俊阳"≠"刘俊源"），确认无关后剔除；
- 整份文件零命中时不可直接下结论：换 dpi 重扫一次（150/200 各一次）并抽查数页确认 OCR 可读，仍零命中才向用户报告"未找到"，由用户确认；
- OCR 仅用于定位候选页，不替代第 3 步的视觉确认。

### 3. 视觉确认与页面归属判定（关键，不可跳过）

对 manifest 中每个文件的每张已渲染页面图片，用 Read 工具逐页查看：

1. **确认姓名**：页面上确实出现用户本人姓名（注意同音字、形近字、打印模糊）。文字层命中的页也要抽看核对；
2. **判定证明边界**：一份文件常含多份证明。含姓名的页属于哪份证明，看页面标题 / 落款 / 盖章判断；
3. **跨页证明**：若一份证明有两页（典型：第 1 页证书正文、第 2 页盖章 / 名单 / 附页），两页都纳入该证明。相邻页已在第 2 步渲染，直接查看判断；需要更多页时用 `--render` 补渲染；
4. **剔除无关页**：不含姓名且不属于其证明跨页的页面不纳入。

任何无法确定的地方（姓名存疑、页归属存疑、一份证明是否缺页）必须向用户说明并询问，禁止臆断。

### 4. 分类与判定分值

阅读每份确认的证明内容（活动名称、级别、时间、颁发单位），对照细则归类并定分值：

- 类别名称沿用细则原文编号与标题；
- 分值按细则条款给出，注明依据（写入 plan 的 `note` 字段，如"校级一等奖 +3，细则第二条第3款"）；
- 同一活动多份证明不重复计分；超出细则封顶的按封顶计并在 note 说明；
- 无法归类的证明单独列出，向用户报告后由用户决定去留。

### 5. 生成汇总 Word（scripts/build_docx.py）

先写 plan JSON（结构见脚本 docstring，images 用绝对路径、按页序排列）：

```json
{
  "title": "本科生综合素质测评活动证明汇总",
  "student_name": "<姓名>",
  "categories": [
    {"name": "一、思想品德", "entries": [
      {"activity": "...", "score": 2, "note": "...", "images": ["p1.png", "p2.png"]}
    ]}
  ]
}
```

再生成：

```bash
python scripts/build_docx.py --plan <plan.json> --out <输出目录>/综测证明汇总.docx
```

产出 Word 结构：标题 + 姓名 → 加分汇总表（序号 / 活动名称 / 所属类别 / 分值 / 备注 + 合计行）→ 按类别分章 → 每份证明每页独占一页，页首为"活动名称（类别 | 分值：X 分）"标题。

### 6. 交付

用 present_files 交付生成的 Word，并在回复中列出：各类别条数与分值、合计分、被剔除 / 存疑的页面及原因。

## 注意事项

- 姓名匹配是核心正确性要求：宁可多问用户，不可把他人证明混入；
- 证明文件可能涉及个人隐私，提取的中间图片保留在用户工作目录，不外传；
- 细则每次由用户提供，不内置；若用户提到细则修订，以最新提供的版本为准。
