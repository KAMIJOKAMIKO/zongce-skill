pymupdf>=1.24
python-docx>=1.1
pillow>=11.3.0
rapidocr_onnxruntime>=1.3
numpy>=1.26
# 以下仅 Windows + 本机 MS Word 环境需要（处理 .docx/.doc 输入）
docx2pdf>=0.1.8; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
